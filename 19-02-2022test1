count prime numbers
def isprime(n):
    for i in range (2,n):
        if n%i==0:
            return False
    return True

t=int(input("Enter the number of times you want to perform count prime numbers operation:"))
for i in range(t):
    n=input("Enter the range :").split()
    x=int(n[0])
    y=int(n[1])
    count=0
    for i in range(x+1,y):
        if isprime(i):
            count=count+1
    print(count)


    
